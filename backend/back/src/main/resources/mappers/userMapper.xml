<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kdt_final.back.user.dao.UserMapper">

    <select id="findUserAll"
            resultType="com.kdt_final.back.user.domain.User">
        SELECT userId, loginId,password, userName, userNickname, email, birthday, phoneNum
        FROM user
    </select>

    <insert id="createUser"
            parameterType="com.kdt_final.back.user.domain.User">
        INSERT INTO user(loginId,password, userName, userNickname, email, birthday, phoneNum)
        VALUES
       (#{loginId}, #{password}, #{userName}, #{userNickname}, #{email}, #{birthday}, #{phoneNum})

    </insert>

    <select id="findAllByUserNickname"
            parameterType="String"
            resultType="com.kdt_final.back.user.domain.User">
        SELECT  userNickname
        FROM user where userNickname =#{userNickname}
    </select>

    <select id="findAllByLoginId"
            parameterType="String"
            resultType="com.kdt_final.back.user.domain.User">
        SELECT  loginId
        FROM user where loginId =#{loginId}
    </select>


    <select id="login"
            parameterType="com.kdt_final.back.user.domain.User">
         SELECT loginId,password
          FROM user
    </select>

</mapper>