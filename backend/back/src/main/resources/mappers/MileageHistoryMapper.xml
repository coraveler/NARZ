<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kdt_final.back.shop.dao.MileageHistoryMapper">
    <!-- 마일리지 내역 추가 -->
    <insert insert id="insertMileageHistory" parameterType="com.kdt_final.back.shop.domain.MileageHistory">
    INSERT INTO mileage_history (user_id, mileage_points, description, created_at)
    VALUES (#{userId}, #{mileagePoints}, #{description}, CURRENT_TIMESTAMP)
    </insert>

    <!-- 특정 유저의 마일리지 내역 조회 -->
    <select id="findByUserId" parameterType="String" resultType="com.kdt_final.back.shop.domain.MileageHistory">
        SELECT mileage_id, user_id, mileage_points, description, change_date
        FROM mileage_history
        WHERE user_id = #{userId}
    </select>

    <!-- 모든 마일리지 내역 조회 -->
    <select id="getAllHistories" resultType="com.kdt_final.back.shop.domain.MileageHistory">
        SELECT mileage_id, user_id, mileage_points, description, change_date
        FROM mileage_history
    </select>
</mapper>
